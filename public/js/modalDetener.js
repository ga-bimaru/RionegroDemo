// Esta función muestra el modal de confirmación para detener/finalizar el alquiler de una mesa.

export function mostrarModalDetener(mesaId, onConfirm) {
    // ...implementación existente de mostrarModalDetener...
    // Debe mostrar el modal y llamar a onConfirm() si el usuario confirma.
}
